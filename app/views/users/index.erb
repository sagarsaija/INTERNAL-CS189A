<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <h1><%= link_to root_path do %>Mezzo<% end %></h1>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if current_user %>
            <li>Signed in as: <%= current_user.name %> - </li>
            <li><%= link_to "Sign Out", 'sign_out', method: :delete %></li>
        <% else %>
            <li>Sign in<%= link_to "auth/facebook" do %><%= image_tag('facebook.png', width: '40', height: '40', class: 'img-circle') %><% end %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<h1>List of users</h1>

<table border="1">
  <thead>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th>uid</th>
    <th>Instruments</th>
    <th>Looking for</th>
  </tr>
  </thead>

  <% @users.each do |user| %>
      <tr>
        <td><%= link_to user.name%></td>
        <td><%= link_to user.email%></td>
        <td><%= link_to user.uid%></td>
        <td><%= link_to user.instruments%></td>
        <td><%= link_to user.looking_for%></td>
      </tr>
  <% end -%>
</table>









